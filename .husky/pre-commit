echo "🔍 Running pre-commit checks…"

echo "→ format:check"
pnpm format:check || {
  echo "✗ Formatting check failed. Run 'pnpm format' and re-stage."
  exit 1
}

echo "→ build"
pnpm build || {
  echo "✗ Build failed. Fix the errors above before committing."
  exit 1
}

echo "✓ Pre-commit checks passed."
