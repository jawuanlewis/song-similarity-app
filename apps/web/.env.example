# Backend origin for the deployed frontend (no trailing slash).
#
# Leave this UNSET for local development — requests then hit "/api/*" and the
# Vite dev proxy forwards them to the local backend on :3001.
VITE_API_BASE_URL=
